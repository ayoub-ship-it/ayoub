{% extends 'with_sidebar.twig' %}

{% block main %}
    <h1>Produits</h1>
    <p>{{ 'ici on affiche les produits' | markdown | striptags }}
    </p>
    <ul>
    {% for p in item %}
    <li>{{ p.name }}</li>
    {% endfor %}
    </ul>
{% endblock %}